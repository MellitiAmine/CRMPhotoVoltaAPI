namespace CrmPhotoVolta.Domain.Core;

/// <summary>Semantic role kind for authorization and routing (not the display name).</summary>
public enum RoleType
{
    Unknown = 0,
    Admin = 1,
    Manager = 2,
    Commercial = 3,
    Technicien = 4
}
